DB_URL=jdbc:postgresql://localhost:5432/store
DB_USER=store_user
DB_PASSWORD=store_pass

# Redis connection URL (includes auth if needed)
# Local Docker: redis://localhost:6379
# Railway: use ${{Redis.REDIS_URL}} reference
REDIS_URL=redis://localhost:6379

SPRING_CACHE_TYPE=redis

# For single-app deployment, set this to your app's public URL
WEBSITE_URL=http://localhost:8080

COOKIE_SECURE=false
CORS_ALLOWED_ORIGINS=http://localhost:5173

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET_KEY=

JWT_SECRET=
ACCESS_EXPIRATION=
REFRESH_EXPIRATION=
GUEST_EXPIRATION=
